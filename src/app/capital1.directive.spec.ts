import { Capital1Directive } from './capital1.directive';

describe('Capital1Directive', () => {
  it('should create an instance', () => {
    const directive = new Capital1Directive();
    expect(directive).toBeTruthy();
  });
});
